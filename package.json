{
  "name": "virtual-ethernet-switch",
  "version": "0.1.0",
  "description": "Virtual Ethernet switch for Mac OS 8 emulator — provides ARP, ICMP, TCP/HTTP proxy via WebSocket",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "arp": "CHECKPOINT=2 node server.js",
    "tcp": "CHECKPOINT=3 node server.js",
    "proxy": "CHECKPOINT=4 node server.js"
  },
  "dependencies": {
    "ws": "^8.17.0"
  },
  "engines": {
    "node": ">=18"
  }
}
